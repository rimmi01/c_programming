#include <stdio.h>
int main()
{
    // int n;
    // float f;
    // scanf("%d%f", &n, &f);
    // printf("%d %f", n, f);
    // return 0;

    // int n;
    // long int m;
    // float f;
    // scanf("%d%F%ld", &n, &f, &m);
    // printf("%d %f %ld", n, f, m);

    // int n;
    // char c;
    // scanf("%d %c",&n, &c);  // Giving the space between the format specifiers is a good practice, for using char
    // printf("%d %d", n, c);
    // You are using GCC

    // char class_section = 'A';
    // int roll_number = 30;
    // printf("Roll number %d in section %c\n", roll_number, class_section);

    float f;
    scanf("%f", &f);
    printf("%d", f);
    
}