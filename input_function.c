#include <stdio.h>
int main()
{
    int n;
    float f;
    scanf("%d%f", &n, &f);
    return 0;
}

// Three types of inputs we will study in c
// string input
// string input stream
// standard input stream
